config server: config server provides a client-server architecture ,
basically we store the  config details which are common to all the services

Config server itself is a microservice

using config server we can read the config details from gitHub

git-----config server---user/rating/hotel services